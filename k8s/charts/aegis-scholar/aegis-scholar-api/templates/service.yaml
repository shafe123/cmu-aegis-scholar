apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegis-scholar-api.fullname" . }}
  labels:
    {{- include "aegis-scholar-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aegis-scholar-api.selectorLabels" . | nindent 4 }}
