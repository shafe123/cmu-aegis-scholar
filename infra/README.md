Infra (Terraform)
------------------

Place Terraform configuration under this folder. Prefer `infra/modules/` for reusable modules and `infra/envs/<env>/` for environment-specific tfvars and backend configuration.
